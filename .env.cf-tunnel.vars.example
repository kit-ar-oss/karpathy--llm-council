# Cloudflare Tunnel container environment variables
#
# Copy this file to `.env.cf-tunnel.vars` and fill in values.
#
# NOTE: `.env.cf-tunnel.vars` is intentionally gitignored.

# Token from `cloudflared tunnel token <tunnel-name>` (or Cloudflare dashboard).
CLOUDFLARE_TUNNEL_TOKEN=
